- flash.each do |key, value|
  - alert_class = flash_class(key)
  .w-100{class: "alert alert-#{alert_class}"}
    %button{type: "button", class:"close", 'data-dismiss': "alert", 'aria-label': "Close"}
      %span{'aria-hidden': "true"} &times;
    = value.html_safe